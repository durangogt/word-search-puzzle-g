import { useEffect, useState } from 'react';
import { Card } from '@/components/ui/card';
import { Card } from '@/components/ui/card';


  const [time, setTim
  useEffect(() => {
 

    const startTime = Date.now() - time * 1000;
      const elapsed = Math.floor((Date


  }, [isRunning, time
  const formatTim
    const sec
  };

      <TimerIcon size={24} className="text-prim
        {formatTime(time)}
    </Card>
}





















